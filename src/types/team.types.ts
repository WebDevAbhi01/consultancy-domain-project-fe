export interface TeamMember {
  id: string
  name: string
  role: string
  photoUrl: string
  bio: string
}

export interface Milestone {
  id: string
  year: string
  title: string
  description: string
}

export interface CompanyValue {
  id: string
  title: string
  description: string
  icon: string 
}
 